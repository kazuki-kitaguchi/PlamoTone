<h2>アカウント設定</h2>
<%= render "devise/shared/error_messages", resource: resource %>

<div class="edit-form">
  <h3>パスワードの変更</h3>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>

    <div class="field">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "E-mail" %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="field">
      <%= f.password_field :password, autocomplete: "new-password", placeholder: "New Password (#{@minimum_password_length} characters minimum)" %>
    </div>

    <div class="field">
      <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "New Password Confirmation" %>
    </div>

    <div class="field">
      <%= f.password_field :current_password, autocomplete: "current-password",placeholder: "Current Password" %>
    </div>

    <div class="actions">
      <%= f.submit "更新", class: "submit" %>
    </div>
  <% end %>

  <h3>退会の手続き</h3>

  <p>退会は下のボタンから可能です。 <%= button_to "退会する", registration_path(resource_name), data: { confirm: "退会の手続きを行います。よろしいですか?" }, method: :delete, class: "submit" %></p>
</div>
<%= link_to "戻る", :back %>
